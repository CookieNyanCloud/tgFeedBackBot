version: '3.8'

services:

  redis:
    image: 'redis:latest'
    env_file:
      - .env

  tg-sota-feedback:
    image: tg-sota-feedback
    build: ./
    depends_on:
      - redis
    env_file:
      - .env
